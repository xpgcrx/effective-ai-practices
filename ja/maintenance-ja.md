# AIツールメンテナンスのTips

## メモリファイルを分割する

一つのメモリファイルに全てを書いていると管理のしづらさや、コンテキスト圧迫に繋がる。
そのため話題ごとにメモリファイルを分割することが有効。

例：

- `GEMINI.md` に全般的なことを記載
- コードレビュー観点については、`code-review.md` に切り出し、コードレビュー時にはそちらを参照するように `GEMINI.md` に記載

## ツール間でメモリファイルを共用する

https://agents.md/
